#!/bin/sh

case $BLOCK_BUTTON in
    1) notify-send "$(echo "Right click to update..." && checkupdates | head)" ;; 
    3) $TERMINAL -e pacupgrade ;;
esac

PACS=$(checkupdates | grep -c .)

if [ $PACS -gt 0 ] ; then 
    echo " $PACS"
    echo "$PACS"
    echo "#fabd2f"
fi
