# Command shortcuts
alias mkd='mkdir -pv'
alias v='nvim'
alias vim='nvim'
alias q='exit'
alias la='ls -Agoh'
alias ll='ls -goh'
alias gh='history | grep'
alias clip='xclip -selection "clipboard"'

alias todo='topydo'
alias trm='transmission-remote'

# Package manager
alias pkg='sudo eopkg'

# Django
alias runserver='python $(find $(git rev-parse --show-toplevel) -name manage.py) runserver'
alias manage='python $(find $(git rev-parse --show-toplevel) -name manage.py)'

# venv
alias activate='source $(find $(git rev-parse --show-toplevel) -name activate)'

# file shortcuts
alias bpr='nvim ~/.profile'
alias sbpr='source ~/.profile'
alias brc='nvim ~/.bashrc'
alias vrc='nvim ~/.config/nvim/init.vim'
alias bal='nvim ~/.aliases'
alias sbrc='source ~/.bashrc'

# directory shortcuts
alias bin='cd ~/.local/bin && ll'
alias conf='cd ~/.config && ll'
alias dots='cd ~/.dotfiles && ll'
alias wiki='cd ~/Code/wiki && ll'
alias ifb='cd ~/Code/ifb && ll'

# school
alias seclab='ssh -i /home/jake/.ssh/cs455 -N -D 9000 scottj@140.211.102.130'
